# Copy to secrets.env before running: cp secrets.env.example secrets.env
# For development/self-hosting you may leave the default seed. When you submit,
# the proctor will validate your findings against a fresh instance with a
# different, private seed — so hard-coding captured values will not pass.
MLP_SEED=dev-seed-CHANGE-ME
